/**
 * Test case for hasLibraryDiff.
 * Runs with mocha.
 */
'use strict'

const hasLibraryDiff = require('../lib/util/has_library_diff.js')
const assert = require('assert')
const co = require('co')

describe('has-library-diff', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Has library diff', () => co(function * () {

  }))
})

/* global describe, before, after, it */
