/**
 * Test case for commitPush.
 * Runs with mocha.
 */
'use strict'

const commitPush = require('../lib/commit_push.js')
const assert = require('assert')
const co = require('co')

describe('commit-push', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Commit push', () => co(function * () {

  }))
})

/* global describe, before, after, it */
