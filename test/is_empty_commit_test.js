/**
 * Test case for isEmptyCommit.
 * Runs with mocha.
 */
'use strict'

const isEmptyCommit = require('../lib/util/is_empty_commit.js')
const assert = require('assert')
const co = require('co')

describe('is-empty-commit', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Is empty commit', () => co(function * () {

  }))
})

/* global describe, before, after, it */
