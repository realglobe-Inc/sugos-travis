/**
 * Test case for checkCiEnv.
 * Runs with mocha.
 */
'use strict'

const checkCiEnv = require('../lib/util/check_ci_env.js')
const assert = require('assert')
const co = require('co')

describe('check-ci-env', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Check ci env', () => co(function * () {

  }))
})

/* global describe, before, after, it */
