/**
 * Test case for createNpmrc.
 * Runs with mocha.
 */
'use strict'

const createNpmrc = require('../lib/create_npmrc.js')
const assert = require('assert')
const co = require('co')

describe('create-npmrc', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Create npmrc', () => co(function * () {

  }))
})

/* global describe, before, after, it */
